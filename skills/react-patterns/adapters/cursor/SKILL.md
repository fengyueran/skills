---
name: "react-patterns"
description: "React 模式指南在 Cursor 中的适配文件。用于在 Cursor 中判断 React 组件组织、Hook 抽取、状态放置、组合方式和现代 React 模式选择。"
---

# react-patterns（cursor 适配）

在 Cursor 中：

1. 先读取 `../../core/SKILL.core.md`。
2. 再按任务类型读取对应 reference 文件，不默认全量加载。
3. 给出模式建议时，优先选择最小、最稳、最容易维护的方案。
4. 避免把 patterns 回答成风格偏好清单。
